{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "gateway.fullname" .) }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
{{- end }}
