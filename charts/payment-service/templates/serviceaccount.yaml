{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "payment-service.fullname" .) }}
  labels:
    {{- include "payment-service.labels" . | nindent 4 }}
{{- end }}
