{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "order-service.fullname" .) }}
  labels:
    {{- include "order-service.labels" . | nindent 4 }}
{{- end }}
