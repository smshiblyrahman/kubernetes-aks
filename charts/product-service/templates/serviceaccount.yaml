{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "product-service.fullname" .) }}
  labels:
    {{- include "product-service.labels" . | nindent 4 }}
{{- end }}
