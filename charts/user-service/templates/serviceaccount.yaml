{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "user-service.fullname" .) }}
  labels:
    {{- include "user-service.labels" . | nindent 4 }}
{{- end }}
